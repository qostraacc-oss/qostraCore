# Environment Settings
DEBUG=True
SECRET_KEY=django-insecure-z(v$f6#o3^=z-r@q7afk+ki&)a%^xpfy3$4j@+6&iq2iwrzwr&

# Database Settings
# For local SQLite setup, you can leave these as default or comment them out.
# For production/staging databases (e.g. PostgreSQL), configure the settings below.
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432

# Allowed Hosts (comma-separated list for production)
ALLOWED_HOSTS=localhost,127.0.0.1
